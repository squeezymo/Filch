<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Filch.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bg_primary</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <!--        // Use windowSplashScreenAnimatedIcon to add either a drawable or an-->
        <!--        // animated drawable. One of these is required.-->
        <!--        <item name="windowSplashScreenAnimatedIcon">@drawable/...</item>-->
        <!--        <item name="windowSplashScreenAnimationDuration">200</item>  # Required for-->
        <!--        # animated icons-->

        <item name="postSplashScreenTheme">@style/Theme.Filch</item>
    </style>

</resources>
